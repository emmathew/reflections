What happens when you initialize a repository? Why do you need to do it?
Frist step in creating a git repo is git initialization. It is need to create git metadata to maitain the new repo.

How is the staging area different from the working directory and the repository? What value do you think it offers
Staging area a virtual special area provided by git. It helps to have controll on the files to be added under one commit